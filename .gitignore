# Runtime data and secrets
.env
.env.*
!.env.example
.secret_key
*.secret_key
*.db
*.sqlite

# Application data (DB, uploaded files). Anchored: without the leading `/`, the
# pattern also swallows any source directory named `data` at any depth, and
# `git add -A` skips it without a word. It did — four settings components.
/data/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage

# Frontend
frontend/node_modules/
frontend/dist/
*.tsbuildinfo

# Local artifacts
/local/
/.local/
/tmp/
/reports/
*.log
/vauxtra-dev/
/lab/

# Editor and OS
.DS_Store
*.swp
.idea/
